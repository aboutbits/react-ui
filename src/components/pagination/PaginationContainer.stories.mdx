import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs'
import { PaginationContainer } from './PaginationContainer'

<Meta
  title="Components/Pagination/PaginationContainer"
  component={PaginationContainer}
/>

# PaginationContainer

This component is the root-component for the pagination.

export const Template = ({ children, ...args }) => (
  <PaginationContainer {...args}>{children}</PaginationContainer>
)

<Canvas>
  <Story name="Default">{Template.bind({})}</Story>
</Canvas>

### Props

<ArgsTable story="Default" />

### Theme

- `pagination.container.base`: Define the flexbox and the padding of component.
