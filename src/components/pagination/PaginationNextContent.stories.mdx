import { Meta, Story, Canvas } from '@storybook/addon-docs'
import {
  Theme,
  InternationalizationMessages,
} from '../../../.storybook/components'
import { PaginationNextContent } from './PaginationPreviousNextContent'

<Meta
  title="Components/Pagination/PaginationNextContent"
  component={PaginationNextContent}
/>

# PaginationNextContent

This component is used to generate the arrow next, to a following page, within the pagination.

export const Template = ({ ...args }) => <PaginationNextContent {...args} />

<Canvas>
  <Story name="Default">{Template.bind({})}</Story>
</Canvas>

### Theme

<Theme component="pagination" items={['prevNext']} />

### Internationalization messages

<InternationalizationMessages items={['pagination.next']} />
