import { Meta, Story, Canvas } from '@storybook/addon-docs'
import {
  Theme,
  InternationalizationMessages,
} from '../../../.storybook/components'
import { PaginationPreviousContent } from './PaginationPreviousNextContent'

<Meta
  title="Components/Pagination/PaginationPreviousContent"
  component={PaginationPreviousContent}
/>

# PaginationPreviousContent

This component is used to generate the arrow back, to a previous page, within the pagination.

export const Template = ({ ...args }) => <PaginationPreviousContent {...args} />

<Canvas>
  <Story name="Default">{Template.bind({})}</Story>
</Canvas>



<Theme component="pagination" items={['prevNext']} />

<InternationalizationMessages items={['pagination.prev']} />
