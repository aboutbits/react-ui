import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs'

import { Warning } from '../../../svg/Warning'
import { ContentError } from './ContentError'

<Meta title="Components/Content/ContentError" component={ContentError} />

# ContentError

This component renders an error with a reload page button.
This is very useful if your page has only one section.

export const Template = ({ children }) => (
  <ContentError icon={<Warning />}>{children}</ContentError>
)

<Canvas>
  <Story
    name="Default"
    args={{
      children: 'Failed to connect with server.',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

### Props

<ArgsTable story="Default" />

### Theme

- `content.error.base`: Define the basic style of the component.
- `content.error.icon.base`: Define the style of the icon.
- `content.error.title.base`: Define the style of the title.
- `content.error.children.base`: Define the style of the passed children.

### Internationalization

- `shared.error.title`
- `shared.button.reload`
