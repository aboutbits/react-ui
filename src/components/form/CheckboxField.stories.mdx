import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs'
import { CheckboxField } from './CheckboxField'
import { Status } from './types'

<Meta title="Components/Form/CheckboxField" component={CheckboxField} />

# CheckboxField

A checkbox field.

It is composed of the primitives [Checkbox](../?path=/docs/components-form-primitive-checkbox--default-story) and [InputMessage](../?path=/docs/components-form-primitive-inputmessage--default-story).

export const Template = ({ ...args }) => <CheckboxField {...args} />

<Canvas>
  <Story
    name="Default"
    args={{
      label: 'Name',
      disabled: false,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

### Props

<ArgsTable story="Default" />

## With message

<Canvas>
  <Story
    name="With message"
    args={{
      label: 'Label',
      message: 'This is the message',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Status invalid

<Canvas>
  <Story
    name="Status invalid"
    args={{
      label: 'Label',
      message: 'This is the message',
      status: Status.invalid,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Disabled

<Canvas>
  <Story
    name="Disabled"
    args={{
      label: 'Label',
      message: 'This is the message',
      disabled: true,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>
