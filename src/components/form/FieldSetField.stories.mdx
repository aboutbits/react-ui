import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs'
import { FieldSetField } from './FieldSetField'
import { Status } from './types'

<Meta title="Components/Form/FieldSetField" component={FieldSetField} />

# FieldSetField

A fieldset field independent of any form validation library.

export const Template = ({ ...args }) => <FieldSetField {...args} />

<Canvas>
  <Story
    name="Default"
    args={{
      label: 'Label',
      children: <>FieldSet Content</>,
    }}
    argTypes={{
      disabled: { type: 'boolean' },
      children: { control: { disable: true } },
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

### Props

<ArgsTable story="Default" />

## With message

<Canvas>
  <Story
    name="With message"
    args={{
      label: 'Label',
      children: <>FieldSet Content</>,
      message: 'This is the message',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Status invalid

<Canvas>
  <Story
    name="Status invalid"
    args={{
      label: 'Label',
      message: 'This is the message',
      status: Status.invalid,
      children: <>FieldSet Content</>,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Disabled

<Canvas>
  <Story
    name="Disabled"
    args={{
      label: 'Label',
      message: 'This is the message',
      disabled: true,
      children: <>FieldSet Content</>,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>
