import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs'
import { InternationalizationMessages } from '../../../.storybook/components'
import { SelectMonthField } from './SelectMonthField'

<Meta title="Components/Form/SelectMonthField" component={SelectMonthField} />

# SelectMonthField

A select field for months independent of any form validation library.

It uses the [SelectField](../?path=/docs/components-form-selectfield--docs) and [Option](../?path=/docs/components-form-primitive-option--docs) components.

export const Template = (args) => <SelectMonthField {...args} />

<Canvas>
  <Story
    name="Default"
    args={{
      label: 'Month of birth',
    }}
    argTypes={{
      disabled: { type: 'boolean' },
      required: { type: 'boolean' },
      hideRequired: { type: 'boolean' },
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

### Props

<ArgsTable story="Default" />

<InternationalizationMessages
  items={[
    'month.january',
    'month.february',
    'month.march',
    'month.april',
    'month.may',
    'month.june',
    'month.july',
    'month.august',
    'month.september',
    'month.october',
    'month.november',
    'month.december',
  ]}
/>
