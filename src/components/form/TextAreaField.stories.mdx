import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs'
import { Mode } from '../types'
import { TextAreaField } from './TextAreaField'
import { FormVariant, Status } from './types'

<Meta title="Components/Form/TextAreaField" component={TextAreaField} />

# TextAreaField

A text area field independent of any form validation library.

It is composed of the primitives [TextArea](../?path=/docs/components-form-primitive-textarea--docs), [InputLabel](../?path=/docs/components-form-primitive-inputlabel--docs) and [InputMessage](../?path=/docs/components-form-primitive-inputmessage--docs).

export const Template = ({ ...args }) => <TextAreaField {...args} />

<Canvas>
  <Story
    name="Default"
    args={{
      label: 'Bio',
      placeholder: 'Your Bio',
      message: 'Say something about yourself',
      mode: Mode.Light,
      variant: FormVariant.solid,
      disabled: false,
      rows: 4,
    }}
    argTypes={{
      required: { type: 'boolean' },
      hideRequired: { type: 'boolean' },
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

### Props

<ArgsTable story="Default" />

## With label

<Canvas>
  <Story
    name="With label"
    args={{
      label: 'Label',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## With message

<Canvas>
  <Story
    name="With message"
    args={{
      message: 'This is the message',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## With label and message

<Canvas>
  <Story
    name="With label and message"
    args={{
      label: 'Label',
      message: 'This is the message',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Status invalid

<Canvas>
  <Story
    name="Status invalid"
    args={{
      label: 'Label',
      message: 'This is the message',
      status: Status.invalid,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Disabled

<Canvas>
  <Story
    name="Disabled"
    args={{
      label: 'Label',
      message: 'This is the message',
      disabled: true,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>
