import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs'
import { action } from '@storybook/addon-actions'
import { Formik } from 'formik'
import * as Yup from 'yup'
import { Mode } from '../types'
import { TextArea } from './TextArea'
import { Form } from './Form'

<Meta
  title="Components/Form/TextArea"
  component={TextArea}
  decorators={[
    (Story) => {
      const validationSchema = Yup.object().shape({
        description: Yup.string().required('Required'),
      })
      return (
        <Formik
          initialValues={{ right: '' }}
          validateOnChange={true}
          validationSchema={validationSchema}
          onSubmit={(values) => action('onSubmit')(values)}
        >
          <Form>
            <Story />
          </Form>
        </Formik>
      )
    },
  ]}
/>

# TextArea

This component can be used to create textarea fields. This input already has a label and handles error messages in `formik` Forms.

export const Template = ({ ...args }) => (
  <TextArea {...args} name="description" />
)

<Canvas>
  <Story
    name="Default"
    args={{
      label: 'Description',
      rows: 4,
      cols: 5,
      maxlength: 240,
      spellcheck: true,
      mode: Mode.light,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

### Props

<ArgsTable story="Default" />

### Theme

The TextArea component shares the styles with the Input component.
For customization of the styles please checkout the [theme section](/docs/components-form-input--default-story#theme) of the Input component.
