import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs'

import { Formik } from 'formik'
import { Form } from './Form'

<Meta
  title="Components/Form/Form"
  component={Form}
  decorators={[
    (Story) => (
      <Formik
        onSubmit={(values) => {
          console.log(values)
        }}
        initialValues={{ email: '' }}
      >
        <Story />
      </Formik>
    ),
  ]}
/>

# Form

This component is a wrapper around [Formik's form](https://formik.org/docs/api/form) component.
It also applies a gap between its children.

export const Template = ({ children, ...args }) => (
  <Form {...args}>{children}</Form>
)

<Canvas>
  <Story
    name="Default"
    args={{
      children: (
        <>
          <div className="bg-gray-500 p-4" />
          <div className="bg-gray-100 p-4" />
          <div className="bg-gray-500 p-4" />
        </>
      ),
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

### Props

<ArgsTable story="Default" />

### Theme

- `form.form.base`: Defines base styles of the form.
