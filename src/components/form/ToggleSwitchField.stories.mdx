import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs'
import { ToggleSwitchField } from './ToggleSwitchField'
import { Status } from './types'

<Meta title="Components/Form/ToggleSwitchField" component={ToggleSwitchField} />

# ToggleSwitchField

A toggle switch field.

Is composed of the primitives [ToggleSwitch](../?path=/docs/components-form-primitive-toggleswitch--default-story) and [InputMessage](../?path=/docs/components-form-primitive-inputmessage--default-story).

export const Template = ({ ...args }) => <ToggleSwitchField {...args} />

<Canvas>
  <Story
    name="Default"
    args={{
      label: 'Airplane mode',
    }}
    argTypes={{
      disabled: { type: 'boolean' },
      required: { type: 'boolean' },
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

### Props

<ArgsTable story="Default" />

## With message

<Canvas>
  <Story
    name="With message"
    args={{
      label: 'Label',
      message: 'This is the message',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Status invalid

<Canvas>
  <Story
    name="Status invalid"
    args={{
      label: 'Label',
      message: 'This is the message',
      status: Status.invalid,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Disabled

<Canvas>
  <Story
    name="Disabled"
    args={{
      label: 'Label',
      message: 'This is the message',
      disabled: true,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>
