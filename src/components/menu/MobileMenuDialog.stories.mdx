import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs'
import { MenuProvider } from '../header/areas/MenuContext'
import { HeaderMain } from '../header'
import { MobileMenuDialog } from './MobileMenuDialog'
import { DrawerContainer } from './DrawerContainer'

<Meta title="Components/Menu/MobileMenuDialog" component={MobileMenuDialog} />

# MobileMenuDialog

This component is used to provide a mobile menu.

It leverages on the component [MobileMenuDialogCloseButton](/docs/components-menu-MobileMenuDialogCloseButton--default-story)

_**Important**_: this storybook is only available for a mobile view. Go to Canvas -> change the size of the preview -> Small mobile.

export const Template = ({ ...args }) => {
  return (
    <MenuProvider>
      <DrawerContainer>
        <HeaderMain title="Only mobile view!" />
        <MobileMenuDialog {...args} dialogLabel="Mobile Menu" />
      </DrawerContainer>
    </MenuProvider>
  )
}

<Canvas>
  <Story name="Default">{Template.bind({})}</Story>
</Canvas>

### Props

<ArgsTable story="Default" />

### Theme

- `menu.mobile.base`: Define the positioning, and the background gradiant for the mobile menu.
