import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs'
import IconArrowDropDownCircle from '@aboutbits/react-material-icons/dist/IconArrowDropDownCircle'
import { MenuNavigation } from './MenuNavigation'
import { MenuButton } from './MenuButton'

<Meta title="Components/Menu/MenuNavigation" component={MenuNavigation} />

# MenuNavigation

This component is used to provide an HTML-`nav` element for a menu.

export const Template = ({ ...args }) => (
  <MenuNavigation navLabel="Navigation" {...args}>
    <MenuButton text="Button 1" icon={IconArrowDropDownCircle} />
    <MenuButton text="Button 2" icon={IconArrowDropDownCircle} />
    <MenuButton text="Button 3" icon={IconArrowDropDownCircle} />
  </MenuNavigation>
)

<Canvas>
  <Story name="Default">{Template.bind({})}</Story>
</Canvas>

### Props

<ArgsTable story="Default" />

### Theme

- `menu.container.base`: Define the flexbox and padding between each child element.
