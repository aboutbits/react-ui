import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs'
import { MenuProvider } from '../header/areas/MenuContext'
import { HeaderMain } from '../header'
import { MobileMenu } from './MobileMenu'
import { MenuContainer } from './MenuContainer'

<Meta title="Components/Menu/MobileMenu" component={MobileMenu} />

# MobileMenu

This component is used to provide a mobile menu.

It leverages on the component [MobileMenuCloseButton](/docs/components-menu-mobilemenuclosebutton--default-story)

_**Important**_: this storybook is only available for a mobile view. Go to Canvas -> change the size of the preview -> Small mobile.

export const Template = ({ ...args }) => {
  return (
    <MenuProvider>
      <MenuContainer>
        <HeaderMain title="Only mobile view!" />
        <MobileMenu {...args} dialogLabel="Mobile Menu" />
      </MenuContainer>
    </MenuProvider>
  )
}

<Canvas>
  <Story name="Default">{Template.bind({})}</Story>
</Canvas>

### Props

<ArgsTable story="Default" />

### Theme

- `menu.mobile.base`:
