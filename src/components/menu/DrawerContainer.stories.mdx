import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs'
import { DrawerContainer } from './DrawerContainer'

<Meta title="Components/Menu/DrawerContainer" component={DrawerContainer} />

# DrawerContainer

This component is used to generate a container for a menu, its main purpose is to apply the shadow and the flexbox.

export const Template = ({ ...args }) => (
  <DrawerContainer {...args} className="py-4" />
)

<Canvas>
  <Story name="Default">{Template.bind({})}</Story>
</Canvas>

### Props

<ArgsTable story="Default" />

### Theme

- `menu.container.base`: Define the flexbox and show for the DrawerContainer.
- `menu.container.normal`: Define the background color for the DrawerContainer.
