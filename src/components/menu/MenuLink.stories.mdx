import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs'
import IconAccountCircle from '@aboutbits/react-material-icons/dist/IconAccountCircle'
import { MenuLink } from './MenuLink'

<Meta title="Components/Menu/MenuLink" component={MenuLink} />

# MenuLink

This component is used to added to a [Menu](/docs/components-menu-menu--default-story).

export const Template = ({ ...args }) => (
  <ul>
    <MenuLink
      {...args}
      href="/?path=/story/components-menu-menulink--default-story"
      text="Button"
      icon={IconAccountCircle}
    />
  </ul>
)

<Canvas>
  <Story name="Default">{Template.bind({})}</Story>
</Canvas>

### Props

<ArgsTable story="Default" />

### Theme

- `menu.button.base`: Define the padding for the button.
- `menu.button.normal`: Define the background color and interaction for hover and focus
- `menu.button.active`: Define the background color and interaction for hover and focus
- `menu.button.inactive`: Define the background color and interaction for hover and focus
- `menu.button.icon.base`: Define the size of the icon.
