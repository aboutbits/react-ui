import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs'
import { Theme } from '../../../../.storybook/components'
import { Fieldset } from './Fieldset'

<Meta title="Components/FormNew/primitive/Fieldset" component={Fieldset} />

# Fieldset

A themed fieldset.

It inherits styles from the label.

export const Template = ({ ...args }) => <Fieldset {...args} />

<Canvas>
  <Story
    name="Default"
    args={{
      label: 'Label',
      children: <>Fieldset Content</>,
    }}
    argTypes={{
      disabled: { type: 'boolean' },
      children: { control: { disable: true } },
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

### Props

<ArgsTable story="Default" />

### Theme

<Theme component="formNew" items={['fieldset']} />
