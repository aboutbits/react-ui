import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs'
import { HeaderArea } from './HeaderArea'
import { HeaderTitle } from './HeaderTitle'

<Meta title="Components/Header/HeaderArea" component={HeaderArea} />

# HeaderArea

This component defines the container of a page header.

export const Template = (args) => <HeaderArea {...args} />

<Canvas>
  <Story
    name="Default"
    args={{ children: 'HeaderTitle' }}
    argTypes={{
      children: {
        options: ['HeaderTitle'],
        mapping: {
          'Header Title': <HeaderTitle>Header Title</HeaderTitle>,
        },
        control: { type: 'select' },
      },
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

### Props

<ArgsTable story="Default" />

### Theme

- `header.area.base`: Define the styles of the header area.
