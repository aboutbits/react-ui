import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs'
import { HeaderTitle } from './HeaderTitle'

<Meta title="Components/Header/HeaderTitle" component={HeaderTitle} />

# HeaderTitle

This component is used for the title of a page. It renders an HTML `h1` element.

export const Template = (args) => <HeaderTitle {...args} />

<Canvas>
  <Story name="Default" args={{ children: 'Header Title' }}>
    {Template.bind({})}
  </Story>
</Canvas>

### Props

<ArgsTable story="Default" />

### Theme

- `header.title.base`: Define the base style of the component.
