import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs'
import { HeaderTitle } from './HeaderTitle'

<Meta title="Components/Header/HeaderTitle" component={HeaderTitle} />

# HeaderTitle

This component uses an HTML-h1 element and some basic styling for the desktop and mobile version.

export const Template = ({ ...args }) => <HeaderTitle {...args} />

<Canvas>
  <Story name="Default" args={{ children: 'Boots Shop' }}>
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Default" />
