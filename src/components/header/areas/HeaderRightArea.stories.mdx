import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs'
import { HeaderRightArea } from './HeaderRightArea'
import { HeaderSmallAction } from '../actions/HeaderSmallAction'
import IconSearch from '@aboutbits/react-material-icons/dist/IconSearch'

<Meta title="Components/Header/HeaderRightArea" component={HeaderRightArea} />

# HeaderRightArea

This component applies a margin to the left. The children are displayed in the same row from left to right.

export const Template = ({ ...args }) => <HeaderRightArea {...args} />

<Canvas>
  <Story
    name="Default"
    args={{
      children: (
        <div>
          <input type="search" placeholder="type...." className="w-auto" />
          <HeaderSmallAction
            icon={IconSearch}
            label="start search"
            onClick={() => alert('Clicked on search')}
          />
        </div>
      ),
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Default" />
