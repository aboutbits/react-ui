import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs'
import { HeaderLeftArea } from './HeaderLeftArea'
import { HeaderSmallAction } from '../actions/HeaderSmallAction'
import IconSearch from '@aboutbits/react-material-icons/dist/IconSearch'

<Meta title="Components/Header/HeaderLeftArea" component={HeaderLeftArea} />

# HeaderLeftArea

This component applies a margin to the right. The children are displayed in the same row from left to right.

export const Template = ({ ...args }) => <HeaderLeftArea {...args} />

<Canvas>
  <Story
    name="Default"
    args={{
      children: (
        <div>
          <input type="search" placeholder="type...." className="w-auto" />
          <HeaderSmallAction
            icon={IconSearch}
            label="start search"
            onClick={() => alert('Clicked on search')}
          />
        </div>
      ),
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Default" />
