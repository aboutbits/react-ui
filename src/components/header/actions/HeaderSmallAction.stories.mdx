import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs'
import { HeaderSmallAction } from './HeaderSmallAction'
import Icon from '@aboutbits/react-material-icons/dist/IconSearch'
import IconEdit from '@aboutbits/react-material-icons/dist/IconEdit'

<Meta
  title="Components/Header/HeaderSmallAction"
  component={HeaderSmallAction}
/>

# HeaderSmallAction

This component is used to add a _small_ icon to the header. It applies some basic styling for the icon.

export const Template = ({ ...args }) => (
  <HeaderSmallAction
    {...args}
    icon={Icon}
    label="Enter search term"
    onClick={() => alert('Button got pressed')}
  />
)

<Canvas>
  <Story name="Default">{Template.bind({})}</Story>
</Canvas>

### Props

<ArgsTable story="Default" />

### Theme

- `hover:text-header-action`: Defines the color on hover
- `hover:text-header-action`: Defines the color on focus

## Custom Icon

<Canvas>
  <Story name="Custom Icon">
    <HeaderSmallAction
      icon={IconEdit}
      label="Edit"
      onClick={() => alert('Button got pressed')}
    />
  </Story>
</Canvas>
