import { IconArrowBackIosOutlinedFilled } from '@aboutbits/react-material-icons'
import { IconArrowBackOutlinedFilled } from '@aboutbits/react-material-icons'
import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs'
import { InternationalizationMessages } from '../../../../.storybook/components'
import { HeaderCloseAction } from './HeaderCloseAction'

<Meta
  title="Components/Header/HeaderCloseAction"
  component={HeaderCloseAction}
/>

# HeaderCloseAction

This component is used to add a `close action' on the left side of a header.

It leverages on the work of [ButtonIcon](/docs/components-button-buttonicon--docs), which adds the icon, and [HeaderLeftArea](/docs/components-header-headerleftarea--docs), which is in charge of the positioning.

export const Template = (args) => <HeaderCloseAction {...args} />

<Canvas>
  <Story
    name="Default"
    args={{ icon: 'Default' }}
    argTypes={{
      icon: {
        options: ['Default', 'ArrowBack', 'ArrowBackIos'],
        mapping: {
          Default: undefined,
          ArrowBack: IconArrowBackOutlinedFilled,
          ArrowBackIos: IconArrowBackIosOutlinedFilled,
        },
        control: { type: 'select' },
      },
      onClick: { control: false },
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

### Props

<ArgsTable story="Default" />

<InternationalizationMessages items={['button.close']} />
