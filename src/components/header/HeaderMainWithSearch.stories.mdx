import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs'
import { useState } from 'react'
import { NavigationProvider } from '../navigation/NavigationContext'
import { HeaderMainWithSearch } from './HeaderMainWithSearch'

<Meta
  title="Components/Header/HeaderMainWithSearch"
  component={HeaderMainWithSearch}
/>

# HeaderMainWithSearch

This component is similar to [HeaderMain](/docs/components-header-headermain--default-story), but with the difference that a search button can be found on the right side.

It leverages on the component [HeaderSearch](/docs/components-header-headersearch--default-story).

export const Template = ({ ...args }) => {
  const [search, setSearch] = useState('')
  const clearSearch = () => setSearch('')
  return (
    <NavigationProvider>
      <HeaderMainWithSearch
        {...args}
        search={search}
        actions={{ search: setSearch, clear: clearSearch }}
        label="Open search"
        title="Header Title"
      />
    </NavigationProvider>
  )
}

<Canvas>
  <Story name="Default">{Template.bind({})}</Story>
</Canvas>

### Props

<ArgsTable story="Default" />
