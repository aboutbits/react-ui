import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs'
import { useState } from 'react'
import { HeaderMainWithSearch } from './HeaderMainWithSearch'
import { MenuProvider } from './areas/MenuContext'

<Meta
  title="Components/Header/HeaderMainWithSearch"
  component={HeaderMainWithSearch}
/>

# HeaderMainWithSearch

This component is similar to [HeaderMain](/docs/components-header-headermain--default-story), but with the difference that a search button can be found on the right side.

For the mobile view, an adjusted interface for the search is provided.

export const Template = ({ ...args }) => {
  const [text, setText] = useState('')
  const clearSearch = () => setText('')
  return (
    <MenuProvider>
      <HeaderMainWithSearch
        {...args}
        search={text}
        searchActions={{ search: setText, clear: clearSearch }}
        label="open search"
        title="Header Title"
      />
    </MenuProvider>
  )
}

<Canvas>
  <Story name="Default">{Template.bind({})}</Story>
</Canvas>

### Props

<ArgsTable story="Default" />

### Theme

- `header.mainWithSearch.base`: Define the padding of the component.
- `header.mainWithSearch.normal`: Define the background of the component.
- `header.mainWithSearch.input.base`: Define the padding of the input.
- `header.mainWithSearch.input.normal`: Define the background, placeholder-, and text-color or the input.
- `header.mainWithSearch.button.base`
- `header.mainWithSearch.button.normal`: Define the hover and focus interaction for the button.
- `header.mainWithSearch.icon.base`: Define the default width and height of the icon.

### React Intl

- `app.nav.menu`: Accessibility label for burger menu
- `shared.search.placeholder`: Placeholder text for search field
- `shared.search.clear`: Accessibility label for clear button in the search field
- `shared.search.back`: Accessibility label for return button (mobile only)
