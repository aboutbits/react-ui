import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs'
import { HeaderBackWithoutAction } from './HeaderBackWithoutAction'
import { MenuProvider } from './areas/MenuContext'

<Meta
  title="Components/Header/HeaderBackWithoutAction"
  component={HeaderBackWithoutAction}
/>

# HeaderBackWithoutAction

This component is similar to [HeaderMain](/docs/components-header-headermain--default-story), but with the difference that on the left an arrow back can be found instead of a menu.

export const Template = ({ ...args }) => {
  return (
    <MenuProvider>
      <HeaderBackWithoutAction {...args} title="Header Title" />
    </MenuProvider>
  )
}

<Canvas>
  <Story name="Default">{Template.bind({})}</Story>
</Canvas>

### Props

<ArgsTable story="Default" />

### React Intl

- `app.nav.menu`: Accessibility label for burger menu
- `shared.button.goBack`: Accessibility label for arrow back
