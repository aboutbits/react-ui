import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs'
import { HeaderWithBackNavigation } from './HeaderWithBackNavigation'

<Meta
  title="Components/Header/HeaderWithBackNavigation"
  component={HeaderWithBackNavigation}
/>

# HeaderWithBackNavigation

This component is a convenience component that provides a header with a back button on the left. By default the back button will call the router's back method.

export const Template = (args) => {
  return <HeaderWithBackNavigation {...args} />
}

<Canvas>
  <Story
    name="Default"
    args={{ title: 'Header Title' }}
    argTypes={{ onBack: { control: false } }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

### Props

<ArgsTable story="Default" />
