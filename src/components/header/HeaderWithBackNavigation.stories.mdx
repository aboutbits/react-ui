import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs'
import { NavigationProvider } from '../navigation/NavigationContext'
import { HeaderWithBackNavigation } from './HeaderWithBackNavigation'

<Meta
  title="Components/Header/HeaderWithBackNavigation"
  component={HeaderWithBackNavigation}
/>

# HeaderWithBackNavigation

This component is similar to [Header](/docs/components-header-header--default-story), but with the difference that on the left an arrow back can be found.

It leverages on the component [HeaderBackAction](/docs/components-header-headerbackaction--default-story) and [HeaderArea](/docs/components-header-headerarea--default-story).

export const Template = ({ ...args }) => {
  return (
    <NavigationProvider>
      <HeaderWithBackNavigation {...args} title="Header Title" />
    </NavigationProvider>
  )
}

<Canvas>
  <Story name="Default">{Template.bind({})}</Story>
</Canvas>

### Props

<ArgsTable story="Default" />
