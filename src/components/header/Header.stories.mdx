import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs'
import { Header } from './Header'

<Meta title="Components/Header/Header" component={Header} />

# Header

This component is a convenience component that provides a header with a title.

export const Template = (args) => {
  return <Header {...args} />
}

<Canvas>
  <Story name="Default" args={{ title: 'Header Title' }}>
    {Template.bind({})}
  </Story>
</Canvas>

### Props

<ArgsTable story="Default" />
