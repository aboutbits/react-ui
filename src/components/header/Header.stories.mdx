import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs'
import { Header } from './Header'

<Meta title="Components/Header/Header" component={Header} />

# Header

This component can be used to create swiftly a header.

export const Template = ({ ...args }) => (
  <Header {...args} title="Header Title" />
)

<Canvas>
  <Story name="Default" args={{ children: 'Header Title' }}>
    {Template.bind({})}
  </Story>
</Canvas>

### Props

<ArgsTable story="Default" />
