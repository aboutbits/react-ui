import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs'
import { DialogHeaderWithClose } from './DialogHeaderWithClose'

<Meta
  title="Components/Dialog/DialogHeaderWithClose"
  component={DialogHeaderWithClose}
/>

# DialogHeaderWithClose

This component is used in the [Dialog](/docs/components-dialog-dialog--default) to provide a header with close button.

export const Template = (args) => (
  <DialogHeaderWithClose {...args} onDismiss={() => void 0} />
)

<Canvas>
  <Story
    name="Default"
    args={{
      title: 'Dialog Title',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

### Props

<ArgsTable story="Default" />
