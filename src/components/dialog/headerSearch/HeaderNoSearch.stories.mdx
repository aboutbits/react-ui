import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs'
import { NavigationProvider } from '../../navigation/NavigationContext'
import { HeaderNoSearch } from './HeaderNoSearch'

<Meta
  title="Components/Dialog/HeaderSearch/HeaderNoSearch"
  component={HeaderNoSearch}
/>

# HeaderNoSearch

This component is used to provide a dialog header with the possibility to start a new search.

export const Template = ({ ...args }) => {
  return (
    <NavigationProvider>
      <HeaderNoSearch
        {...args}
        startSearch={() => void 0}
        onClose={() => void 0}
      />
    </NavigationProvider>
  )
}

<Canvas>
  <Story name="Default" args={{ title: 'Title' }}>
    {Template.bind({})}
  </Story>
</Canvas>

### Props

<ArgsTable story="Default" />
