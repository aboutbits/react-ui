import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs'
import IconAdd from '@aboutbits/react-material-icons/dist/IconAdd'
import { actions } from '@storybook/addon-actions'
import { ButtonIcon } from './ButtonIcon'

<Meta title="Components/Button/ButtonIcon" component={ButtonIcon} />

# ButtonIcon

export const Template = ({ ...args }) => (
  <ButtonIcon {...args} onClick={actions('clicked')} />
)

<Canvas>
  <Story name="Default" args={{ icon: IconAdd, disabled: false }}>
    {Template.bind({})}
  </Story>
</Canvas>

### Props

<ArgsTable story="Default" />
