import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs'
import { actions } from '@storybook/addon-actions'
import { Button, Size, Tone, Variant } from './Button'

<Meta title="Components/Button/Button" component={Button} />

# Button

export const Template = ({ children, ...args }) => (
  <Button {...args} onClick={actions('clicked')}>
    {children}
  </Button>
)

<Canvas>
  <Story name="Default" args={{ children: 'Default Button' }}>
    {Template.bind({})}
  </Story>
</Canvas>

### Props

<ArgsTable story="Default" />

All the other externally defined props are

```
React.DetailedHTMLProps<React.ButtonHTMLAttributes<HTMLButtonElement>, HTMLButtonElement>
```

### Theme

- `button.button.base`
- `button.button.size.sm`: Define the size of the small size button.
- `button.button.size.md`: Define the size of the medium size button.
- `button.button.variantTone.solid.base`
- `button.button.variantTone.solid.primary`: Define the color and interactions for the button with the variant solid and tone primary.
- `button.button.variantTone.solid.critical`: Define the color and interactions for the button with the variant solid and tone critical.
- `button.button.variantTone.solid.secondary`: Define the color and interactions for the button with the variant solid and tone secondary.
- `button.button.variantTone.solid.disabled`: Define the status disabled for the button with the variant solid.
- `button.button.variantTone.ghost.base`
- `button.button.variantTone.ghost.primary`: Define the color and interactions for the button with the variant ghost and tone primary.
- `button.button.variantTone.ghost.critical`: Define the color and interactions for the button with the variant ghost and tone critical.
- `button.button.variantTone.ghost.secondary`: Define the color and interactions for the button with the variant ghost and tone secondary.
- `button.button.variantTone.ghost.disabled`: Define the status disabled for the button with the variant ghost.
- `button.button.variantTone.transparent.base`
- `button.button.variantTone.transparent.primary`: Define the color and interactions for the button with the variant transparent and tone primary.
- `button.button.variantTone.transparent.critical`: Define the color and interactions for the button with the variant transparent and tone critical.
- `button.button.variantTone.transparent.secondary`: Define the color and interactions for the button with the variant transparent and tone secondary.
- `button.button.variantTone.transparent.disabled`: Define the status disabled for the button with the variant transparent.

## Variants

The `Variant` determine if there is a background fill and a border. The `transparent` variant, while being transparent, still keep the same size as the other variants.

<Canvas>
  <Story name="VariantSolid">
    <Button variant={Variant.solid}>Button Solid</Button>
  </Story>
  <Story name="VariantGhost">
    <Button variant={Variant.ghost}>Button Ghost</Button>
  </Story>
  <Story name="VariantTransparent">
    <Button variant={Variant.transparent}>Button Transparent</Button>
  </Story>
</Canvas>

## Size

The size determine the padding between the button text and its border.

<Canvas>
  <Story name="SizeMd">
    <Button size={Size.md}>Medium Button</Button>
  </Story>
  <Story name="SizeSm">
    <Button size={Size.sm}>Small Button</Button>
  </Story>
</Canvas>

## Tone

The tone of the button determine it's background color. Given the prop, the component will look at the Tailwind config to determine its color. Given a color name, for example `green`, the component will use the `DEFAULT` variant, that is `green`, for its normale state, and the `700` variant, that is `green-700` for its focus, hover and active states.

For additional information about Tailwind config, please refer to the [Tailwind documentation on colors](https://tailwindcss.com/docs/customizing-colors).

<Canvas>
  <Story name="Primary">
    <Button tone={Tone.primary}>Primary Button</Button>
  </Story>
  <Story name="Secondary">
    <Button tone={Tone.secondary}>Secondary Button</Button>
  </Story>
  <Story name="Critical">
    <Button tone={Tone.critical}>Critical Button</Button>
  </Story>
</Canvas>

## Tone Custom

If the provided colors are not satisfying the needs of a project, it is possible to add your own colors. In the following example, it is moderated what needs to be passed to the function makeTheme.

```
const themeOverwrite = {
  button: {
    button: {
      variantTone: {
        solid: {
          green: "border-transparent bg-green-500 hover:bg-green-700 focus:bg-green-700 text-white font-bold",
        },
        ghost: {
          green: "border-green-500 hover:border-green-700 text-primary-500 hover:text-green-700",
        },
        transparent: {
          green: "text-green-500 hover:text-green-700",
        },
      }
    }
  }
}
```

<Canvas>
  <Story name="CustomGreen">
    <Button tone="green">Green Button</Button>
  </Story>
</Canvas>

## Full width

<Canvas>
  <Story name="Full width">
    <Button className={'w-full'}>Full width</Button>
  </Story>
</Canvas>
