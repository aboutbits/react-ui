import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs'
import { Button, Size, Tone, Variant } from './Button'

<Meta
  title="Components/Button/Button"
  component={Button}
  argTypes={{ onClick: { action: 'clicked' } }}
/>

# Button

export const Template = ({ children, ...args }) => (
  <Button {...args}>{children}</Button>
)

<Canvas>
  <Story name="Default" args={{ children: 'Default Button' }}>
    {Template.bind({})}
  </Story>
</Canvas>

### Props:

<ArgsTable story="Default" />

All the other externally defined props are

```
React.DetailedHTMLProps<React.ButtonHTMLAttributes<HTMLButtonElement>, HTMLButtonElement>
```

## Variants

The `Variant` determine if there is a background fill and a border. The `transparent` variant, while being transparent, still keep the same size as the other variants.

<Canvas>
  <Story name="VariantSolid">
    <Button variant={Variant.solid}>Button Solid</Button>
  </Story>
  <Story name="VariantGhost">
    <Button variant={Variant.ghost}>Button Ghost</Button>
  </Story>
  <Story name="VariantTransparent">
    <Button variant={Variant.transparent}>Button Transparent</Button>
  </Story>
</Canvas>

## Size

The size determine the padding between the button text and its border.

<Canvas>
  <Story name="SizeMd">
    <Button size={Size.md}>Medium Button</Button>
  </Story>
  <Story name="SizeSm">
    <Button size={Size.sm}>Small Button</Button>
  </Story>
</Canvas>

## Tone

The tone of the button determine it's background color. Given the prop, the component will look at the Tailwind config to determine its color. Given a color name, for example `green`, the component will use the `DEFAULT` variant, that is `green`, for its normale state, and the `700` variant, that is `green-700` for its focus, hover and active states.

For additional information about Tailwind config, please refer to the [Tailwind documentation on colors](https://tailwindcss.com/docs/customizing-colors).

<Canvas>
  <Story name="Primary">
    <Button tone={Tone.primary}>Primary Button</Button>
  </Story>
  <Story name="Secondary">
    <Button tone={Tone.secondary}>Secondary Button</Button>
  </Story>
  <Story name="Critical">
    <Button tone={Tone.critical}>Critical Button</Button>
  </Story>
  <Story name="CustomGreen">
    <Button tone="green">Green Button</Button>
  </Story>
</Canvas>
