import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs'
import { ButtonLink } from './Button'

<Meta title="Components/Button/ButtonLink" component={ButtonLink} />

# ButtonLink

The ButtonLink component has the looks of a Button, but is an anchor tag under the hood.
You can define the type of link in the ReactUIProvider. By default, it will be a plain HTML anchor tag.

export const Template = ({ children, ...args }) => (
  <ButtonLink {...args}>{children}</ButtonLink>
)

<Canvas>
  <Story
    name="Default"
    args={{ children: 'Default ButtonLink', href: '#ButtonLink' }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

### Props

<ArgsTable story="Default" />

All the other externally defined props are

```
AnchorHTMLAttributes<HTMLAnchorElement>
```

### Theme

The themes are the same as for the [Button](/docs/components-button-button--default-story#theme).

## Variants

The variants are the same as for the [Button](/docs/components-button-button--default-story#variants).

## Size

The size is the same as for the [Button](/docs/components-button-button--default-story#size).

## Tone

The tone is the same as for the [Button](/docs/components-button-button--default-story#tone).
