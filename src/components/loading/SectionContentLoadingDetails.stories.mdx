import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs'

import { Layout } from '../section'
import { SectionContentLoadingDetails } from './SectionContentLoadingDetails'

<Meta
  title="Components/Loading/SectionContentLoadingDetails"
  component={SectionContentLoadingDetails}
/>

# SectionContentLoadingDetails

The component is used during the fetch process as a placeholder.
It will display the specified number of [LoadingDescriptionItems](/docs/components-loading-loadingdescriptionitem--default-story) wrapped in a [SectionContent](/docs/components-section-sectioncontent--default-story).

export const Template = ({ ...args }) => (
  <SectionContentLoadingDetails
    {...args}
    numberOfItems={4}
    layout={Layout.twoColumn}
  />
)

<Canvas>
  <Story name="Default">{Template.bind({})}</Story>
</Canvas>

### Props

<ArgsTable story="Default" />
