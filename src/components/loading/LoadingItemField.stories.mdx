import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs'

import { LoadingField } from './LoadingItems'

<Meta title="Components/Loading/LoadingItemField" component={LoadingField} />

# LoadingItemField

This component is used in `LoadingDetails` to generate the placeholders.

export const Template = ({ ...args }) => <LoadingField {...args} />

<Canvas>
  <Story name="Default">{Template.bind({})}</Story>
</Canvas>

### Props

<ArgsTable story="Default" />

## Background Color Primary

<Canvas>
  <Story name="Background Color Primary">
    <LoadingField backgroundColor="primary" />
  </Story>
</Canvas>
