import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs'

import { LoadingDetails } from './LoadingDetails'

<Meta title="Components/Loading/LoadingDetails" component={LoadingDetails} />

# LoadingDetails

The component is used during the fetch process as a placeholder.

It leverages on the work of [SectionContentTwoContainer](/docs/components-section-sectioncontenttwocolumn--default-story).

export const Template = ({ ...args }) => (
  <LoadingDetails {...args} sectionHeader="Section Header!" numberOfItems={2} />
)

<Canvas>
  <Story name="Default">{Template.bind({})}</Story>
</Canvas>

### Props

<ArgsTable story="Default" />

## Color Section Background

<Canvas>
  <Story name="Color Section Background">
    <LoadingDetails
      numberOfItems={2}
      sectionHeader="Section Header!"
      colorSectionBackground="critical"
    />
  </Story>
</Canvas>

## Color Loading Bar

<Canvas>
  <Story name="Color Loading Bar">
    <LoadingDetails
      numberOfItems={2}
      sectionHeader="Section Header!"
      colorLoadingBar="critical"
    />
  </Story>
</Canvas>
