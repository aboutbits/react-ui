import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs'

import { LoadingInput } from './LoadingItems'

<Meta title="Components/Loading/LoadingItemInput" component={LoadingInput} />

# LoadingItemField

This component is used in `LoadingEdit` to generate the placeholders.

export const Template = ({ ...args }) => <LoadingInput {...args} />

<Canvas>
  <Story name="Default">{Template.bind({})}</Story>
</Canvas>

### Props

<ArgsTable story="Default" />

### Theme

- `loading.input.start.base`: Base style of the upper loading bar.
- `loading.input.end.base`: Base style of the lower loading bar.
