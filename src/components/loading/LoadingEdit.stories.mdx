import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs'

import { LoadingEdit } from './LoadingEdit'

<Meta title="Components/Loading/LoadingEdit" component={LoadingEdit} />

# LoadingEdit

The component is used during the fetch process as a placeholder. The contrast to `LoadingDetails` is a different styling.

It leverages on the work of [SectionContentTwoContainer](/docs/components-section-sectioncontenttwocolumn--default-story).

export const Template = ({ ...args }) => (
  <LoadingEdit {...args} sectionHeader="Section Header!" numberOfItems={2} />
)

<Canvas>
  <Story name="Default">{Template.bind({})}</Story>
</Canvas>

### Props

<ArgsTable story="Default" />

## Color Section Background

<Canvas>
  <Story name="Color Section Background">
    <LoadingEdit
      numberOfItems={2}
      sectionHeader="Section Header!"
      colorSectionBackground="critical"
    />
  </Story>
</Canvas>

## Color Loading Background

<Canvas>
  <Story name="Color Loading Background">
    <LoadingEdit
      numberOfItems={2}
      sectionHeader="Section Header!"
      colorLoadingBar="green"
    />
  </Story>
</Canvas>
