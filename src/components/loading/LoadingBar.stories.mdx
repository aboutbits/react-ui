import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs'

import { LoadingBar } from './LoadingBar'

<Meta title="Components/Loading/LoadingBar" component={LoadingBar} />

# LoadingBar

Basic loading component, which is used in all other components to animate the loading process.

export const Template = (args) => <LoadingBar {...args} />

<Canvas>
  <Story name="Default" args={{ className: 'p-4' }}>
    {Template.bind({})}
  </Story>
</Canvas>

### Props

<ArgsTable story="Default" />

### Theme

- `loading.bar.base`: Define the styles of the loading bar.
