import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs'

import { LoadingInput } from './LoadingInput'

<Meta title="Components/Loading/LoadingInput" component={LoadingInput} />

# LoadingInput

This component is a loading skeleton of an Input element.

export const Template = ({ ...args }) => <LoadingInput {...args} />

<Canvas>
  <Story name="Default">{Template.bind({})}</Story>
</Canvas>

### Props

<ArgsTable story="Default" />

### Theme

- `loading.input.upper.base`: Define the padding, and the width for the upper loading bar.
- `loading.input.lower.base`: Define the padding, and the width for the lower loading bar.
