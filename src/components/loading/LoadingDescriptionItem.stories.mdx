import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs'

import { LoadingDescriptionItem } from './LoadingItems'

<Meta
  title="Components/Loading/LoadingDescriptionItem"
  component={LoadingDescriptionItem}
/>

# LoadingDescriptionItem

This component is used to show a loading [SectionDescriptionItem](/docs/components-section-sectiondescriptionitem--default-story).

export const Template = ({ ...args }) => <LoadingDescriptionItem {...args} />

<Canvas>
  <Story name="Default">{Template.bind({})}</Story>
</Canvas>

### Props

<ArgsTable story="Default" />

### Theme

- `loading.descriptionItem.upper.base`: Define the padding, and the width for the upper loading bar.
- `loading.descriptionItem.lower.base`: Define the padding, and the width for the lower loading bar.
