import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs'

import { SectionContentListLoading } from './SectionContentListLoading'

<Meta
  title="Components/Loading/SectionContentListLoading"
  component={SectionContentListLoading}
/>

# LoadingList

This component can be used as placeholders for lists during the fetch process.
Internally it leverages on the component [LoadingListItem](/docs/components-loading-loadinglistitem--default-story) and is wrapped by a [SectionContentList](/docs/components-section-sectioncontentlist--default-story).

export const Template = ({ ...args }) => <SectionContentListLoading {...args} />

<Canvas>
  <Story
    name="Default"
    args={{
      numberOfItems: 3,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

### Props

<ArgsTable story="Default" />
