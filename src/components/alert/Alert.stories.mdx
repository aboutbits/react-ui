import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs'
import { Alert, Tone } from './Alert'

<Meta title="Components/Alert/Alert" component={Alert} />

# Alert

This alert component can be used to
It supports different types of tones.

export const Template = ({ children, ...args }) => (
  <Alert {...args}>{children}</Alert>
)

<Canvas>
  <Story
    name="Default"
    args={{ children: 'Critical Message.', tone: Tone.critical }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

### Props

<ArgsTable story="Default" />

### Theme

- `border-form-message-${tone}`: Define the default border color of the form message per tone.
- `bg-error-message-${tone}`: Define the default background of the form message per tone.
- `text-error-message-content-${tone}`: Define the default color of the form message content per tone.
- `text-error-message-icon-${tone}`: Define the default color of the form message icon per tone.
- `bg-error-message-icon-${tone}`: Define the default background color of the form message icon per tone.

## Tone

<Canvas>
  <Story name="Critical">
    <Alert tone={Tone.critical}>Critical Message</Alert>
  </Story>
  <Story name="Positive">
    <Alert tone={Tone.positive}>Positive Message</Alert>
  </Story>
</Canvas>

## Very Long Message

<Canvas>
  <Story name="Very Long Message">
    <Alert tone={Tone.critical}>
      Duis mauris ligula, aliquam id maximus at, aliquam ut dui. Morbi ac neque
      mattis libero cursus lobortis quis ac urna. Nullam vel augue vitae lacus
      fermentum ullamcorper eu eget sapien.
    </Alert>
  </Story>
</Canvas>

## Without border

<Canvas>
  <Story name="Without border">
    <Alert tone={Tone.critical} className="border-transparent">
      Critical message without border
    </Alert>
  </Story>
</Canvas>
