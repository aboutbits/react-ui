import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs'
import { Button } from '../button'
import { Tone } from '../types'
import { Actions } from './Actions'

<Meta title="Components/Action/Actions" component={Actions} />

# Actions

The `Actions` component positions children in a flex container.
It makes sure that there is space between each item and that they stack on mobile.
This is very handy in a content area or a dialog, where you have two buttons next to each other.

export const Template = ({ children, ...args }) => (
  <Actions {...args}>{children}</Actions>
)

<Canvas>
  <Story
    name="Default"
    args={{ children: 'One button' }}
    argTypes={{
      children: {
        options: ['One button', 'Two buttons'],
        mapping: {
          'One button': <Button>Save</Button>,
          'Two buttons': (
            <>
              <Button tone={Tone.success}>Save</Button>
              <Button>Cancel</Button>
            </>
          ),
        },
        control: { type: 'select' },
      },
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

### Props

<ArgsTable story="Default" />

### Theme

- `action.action.base`: Define the styles of the action.
- `action.action.position.start`: Define left position of children of the action.
- `action.action.position.center`: Define center position of children of the action.
- `action.action.position.end`: Define right position of children of the action.

## Multiple children

<Canvas>
  <Story name="Multiple children">
    <Actions>
      <Button>Button 1</Button>
      <Button>Button 2</Button>
    </Actions>
  </Story>
</Canvas>
