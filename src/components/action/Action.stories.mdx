import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs'
import { Button } from '../../index'
import { Actions } from './Action'

<Meta title="Components/Action/Actions" component={Actions} />

# Action

This component is used to apply positioning to his children quickly.

If there are multiple elements passed as children, they will be placed in a single row.

export const Template = ({ children, ...args }) => (
  <Actions {...args}>{children}</Actions>
)

<Canvas>
  <Story name="Default" args={{ children: <Button>Button 1</Button> }}>
    {Template.bind({})}
  </Story>
</Canvas>

### Props

<ArgsTable story="Default" />

### Theme

- `action.action.base`: Define
- `action.action.variant.left`: Define left positioning of children of the content area.
- `action.action.variant.center`: Define center positioning of children of the content area.
- `action.action.variant.right`: Define right positioning of children of the content area.

## Multiple children

<Canvas>
  <Story name="Multiple children">
    <Actions>
      <Button>Button 1</Button>
      <Button>Button 2</Button>
    </Actions>
  </Story>
</Canvas>
