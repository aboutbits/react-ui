import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs'
import IconFilterList from '@aboutbits/react-material-icons/dist/IconFilterList'
import { action } from '@storybook/addon-actions'
import { SectionAction } from './SectionAction'

<Meta title="Components/Section/SectionAction" component={SectionAction} />

# SectionAction

This component defines an action that can be placed inside a `SectionHeader`.

export const Template = ({ ...args }) => (
  <SectionAction {...args} onClick={action('clicked')} />
)

<Canvas>
  <Story
    name="Default"
    args={{ Icon: IconFilterList, label: 'Filter', disabled: false }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

### Props

<ArgsTable story="Default" />

### Theme

- `section.action.base`: Define the base styling for the section action.
- `section.action.normal`: Define colors for the section action.
- `section.action.disabled`: Define colors for the section action when disabled.
