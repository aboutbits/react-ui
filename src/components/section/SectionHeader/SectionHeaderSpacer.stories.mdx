import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs'
import { Theme } from '../../../../.storybook/components'
import {
  SectionHeaderSpacer,
  SectionHeaderSpacerSize,
} from './SectionHeaderSpacer'

<Meta
  title="Components/Section/SectionHeaderSpacer"
  component={SectionHeaderSpacer}
/>

# SectionHeaderSpacer

This component is used to add a defined space to the bottom of `SectionHeader`. It has to be inserted as the very last element in the `SectionHeader`.

It is best used when you have a multi-row header with search or filters.
Check the [List examples](/docs/examples-list--default-story) to see how to use it.

export const Template = ({ ...args }) => <SectionHeaderSpacer {...args} />

<Canvas>
  <Story
    name="Default"
    args={{
      size: undefined,
    }}
    argTypes={{
      size: {
        options: Object.values(SectionHeaderSpacerSize),
        control: { type: 'select' },
      },
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

### Props

<ArgsTable story="Default" />

### Theme

<Theme component="section" items={['headerSpacer']} />
