import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs'
import { SectionHeaderArea } from './SectionHeaderArea'
import { SectionHeaderTitle } from './SectionHeaderTitle'

<Meta
  title="Components/Section/SectionHeaderArea"
  component={SectionHeaderArea}
/>

# SectionHeaderArea

This component defines the header area of a section.

export const Template = ({ children, ...args }) => (
  <SectionHeaderArea {...args}>{children}</SectionHeaderArea>
)

<Canvas>
  <Story
    name="Default"
    args={{
      children: <SectionHeaderTitle>Profile information</SectionHeaderTitle>,
    }}
    argTypes={{
      children: { control: false },
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

### Props

<ArgsTable story="Default" />

### Theme

- `section.headerArea.base`: Define the base styles for the section heading.
