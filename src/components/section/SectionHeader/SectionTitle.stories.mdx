import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs'
import { SectionTitle } from './SectionHeader'

<Meta title="Components/Section/SectionTitle" component={SectionTitle} />

# SectionTitle

This component is an HTML `h1` tag and defines the style of the section title.

export const Template = ({ children, ...args }) => (
  <SectionTitle {...args}>{children}</SectionTitle>
)

<Canvas>
  <Story name="Default" args={{ children: 'Profile information' }}>
    {Template.bind({})}
  </Story>
</Canvas>

### Props

<ArgsTable story="Default" />

### Theme

- `section.title.base`: Define the default text color and apply the basic styling for the section title.
