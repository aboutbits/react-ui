import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs'
import IconEditOutlinedFilled from '@aboutbits/react-material-icons/dist/IconEditOutlinedFilled'
import { ButtonIcon, ButtonVariant } from '../../button'
import { Size, Tone } from '../../types'
import { Theme } from '../../../../.storybook/components'
import {
  Section,
  SectionContainer,
  SectionContentList,
  SectionListItemLink,
} from '../'

<Meta
  title="Components/Section/SectionListItemLink"
  component={SectionListItemLink}
/>

# SectionListItemLink

This component has the same look and feel as the `SectionListItemButton`, but it renders as a Link component.

export const Template = ({ children, ...args }) => {
  return <SectionListItemLink {...args}>{children}</SectionListItemLink>
}

<Canvas>
  <Story name="Default" args={{ href: '#', children: 'Link' }}>
    {Template.bind({})}
  </Story>
</Canvas>

### Props

<ArgsTable story="Default" />



<Theme component="section" items={['listItemButtonLink']} />

## Without icon

<Canvas>
  <Story name="Without icon">
    <SectionListItemLink withIcon={false}>
      Link
    </SectionListItemLink>
  </Story>
</Canvas>

## With Actions

<Canvas>
  <Story name="With actions">
    <SectionListItemLink actionsArea={
      <ButtonIcon
        icon={IconEditOutlinedFilled}
        variant={ButtonVariant.Transparent}
        size={Size.Sm}
        tone={Tone.Neutral}
      />
    }>
      Link
    </SectionListItemLink>
  </Story>
</Canvas>

## Example: Usage within Section

<Canvas>
  <Story name="Usage within Section">
    <Section>
      <SectionContainer>
        <SectionContentList>
          <SectionListItemLink href="#1">Link 1</SectionListItemLink>
          <SectionListItemLink href="#2" actionsArea={
              <ButtonIcon
                icon={IconEditOutlinedFilled}
                variant={ButtonVariant.Transparent}
                tone={Tone.Neutral}
                size={Size.Sm}
              />
            }>
              Link 2
            </SectionListItemLink>
          <SectionListItemLink href="#3">Link 3</SectionListItemLink>
        </SectionContentList>
      </SectionContainer>
    </Section>
  </Story>
</Canvas>
