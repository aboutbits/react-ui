import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs'
import IconEditOutlinedFilled from '@aboutbits/react-material-icons/dist/IconEditOutlinedFilled'
import { ButtonIcon, ButtonVariant } from '../../button'
import { Size, Tone } from '../../types'
import { Theme } from '../../../../.storybook/components'
import {
  Section,
  SectionContainer,
  SectionContentList,
  SectionListItem,
} from '../'

<Meta title="Components/Section/SectionListItem" component={SectionListItem} />

# SectionListItem

The SectionListItem defines a row in a list of items in a section.

export const Template = ({ children }) => (
  <SectionListItem>{children}</SectionListItem>
)

<Canvas>
  <Story name="Default" args={{ children: 'Jane Doe' }}>
    {Template.bind({})}
  </Story>
</Canvas>

### Props

<ArgsTable story="Default" />



<Theme component="section" items={['listItem']} />

## With icon

<Canvas>
  <Story name="With icon">
    <SectionListItem withIcon>
      Jane Doe
    </SectionListItem>
  </Story>
</Canvas>

## With Actions

<Canvas>
  <Story name="With actions">
    <SectionListItem actionsArea={
      <ButtonIcon
        icon={IconEditOutlinedFilled}
        variant={ButtonVariant.Transparent}
        tone={Tone.Neutral}
        size={Size.Sm}
      />
    }>
      Jane Doe
    </SectionListItem>
  </Story>
</Canvas>

## Example: Usage within SectionContentList

<Canvas>
  <Story name="Usage within Section">
    <Section>
      <SectionContainer>
        <SectionContentList>
          <SectionListItem>Jane Doe</SectionListItem>
          <SectionListItem actionsArea={
              <ButtonIcon
                icon={IconEditOutlinedFilled}
                variant={ButtonVariant.Transparent}
                tone={Tone.Neutral}
                size={Size.Sm}
              />
            }>
              John Doe
            </SectionListItem>
          <SectionListItem>Mary Doe</SectionListItem>
        </SectionContentList>
      </SectionContainer>
    </Section>
  </Story>
</Canvas>
