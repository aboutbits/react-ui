import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs'
import IconEditOutlinedFilled from '@aboutbits/react-material-icons/dist/IconEditOutlinedFilled'
import IconCopyAllOutlinedFilled from '@aboutbits/react-material-icons/dist/IconCopyAllOutlinedFilled'
import { Theme } from '../../../../.storybook/components'
import {
    Section,
    SectionContainer,
    SectionContentList,
    SectionListItemWithAction,
} from '../'

<Meta
  title="Components/Section/SectionListItemWithAction"
  component={SectionListItemWithAction}
/>

# SectionListItemWithAction

The SectionListItemWithAction defines a row in a list of items in a section and pushes an action to right of the content.

export const Template = ({ children, action }) => (
  <SectionListItemWithAction action={action}>
    {children}
  </SectionListItemWithAction>
)

<Canvas>
  <Story
    name="Default"
    args={{
      children: 'Jane Doe',
      action: <IconEditOutlinedFilled width={20} height={20} className="fill-current" />,
    }}
    argTypes={{
      action: { control: false },
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

### Props

<ArgsTable story="Default" />



<Theme component="section" items={['listItemWithAction']} />

## Very Long Text

<Canvas>
  <Story
    name="Very Long Text"
    args={{
      children:
        'This list item has a very long text. The text should wrap, there should be a space between text and action. And the action should not shrink.',
      action: <IconCopyAllOutlinedFilled width={20} height={20} className="fill-current" />,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Example: Usage within SectionContentList

<Canvas>
  <Story name="Usage within Section">
    <Section>
      <SectionContainer>
        <SectionContentList>
          <SectionListItemWithAction
            action={
              <IconEditOutlinedFilled width={20} height={20} className="fill-current" />
            }
          >
            Jane Doe
          </SectionListItemWithAction>
          <SectionListItemWithAction
            action={
              <IconEditOutlinedFilled width={20} height={20} className="fill-current" />
            }
          >
            John Doe
          </SectionListItemWithAction>
          <SectionListItemWithAction
            action={
              <IconEditOutlinedFilled width={20} height={20} className="fill-current" />
            }
          >
            Mary Doe
          </SectionListItemWithAction>
        </SectionContentList>
      </SectionContainer>
    </Section>
  </Story>
</Canvas>
