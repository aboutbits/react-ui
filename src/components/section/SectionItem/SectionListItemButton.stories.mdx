import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs'
import IconEditOutlinedFilled from '@aboutbits/react-material-icons/dist/IconEditOutlinedFilled'
import { Theme } from '../../../../.storybook/components'
import { ButtonIcon, ButtonVariant } from '../../button'
import { Size, Tone } from '../../types'
import {
  Section,
  SectionContainer,
  SectionContentList,
  SectionListItemButton,
} from '../'

<Meta
  title="Components/Section/SectionListItemButton"
  component={SectionListItemButton}
/>

# SectionListItemButton

This component adds an HTML-button element and applies basic styling.

export const Template = ({ children, ...args }) => {
  return <SectionListItemButton {...args}>{children}</SectionListItemButton>
}

<Canvas>
  <Story
    name="Default"
    args={{ children: 'List item button', onClick: { control: false } }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

### Props

<ArgsTable story="Default" />



<Theme component="section" items={['listItemButtonLink']} />

## Without icon

<Canvas>
  <Story name="Without icon">
    <SectionListItemButton withIcon={false}>
      List item button
    </SectionListItemButton>
  </Story>
</Canvas>

## With Actions

<Canvas>
  <Story name="With actions">
    <SectionListItemButton actionsArea={
      <ButtonIcon
        icon={IconEditOutlinedFilled}
        variant={ButtonVariant.Transparent}
        tone={Tone.Neutral}
        size={Size.Sm}
      />
    }>
      List item button
    </SectionListItemButton>
  </Story>
</Canvas>


## Example: Usage within Section

<Canvas>
  <Story name="Usage within Section">
    <Section>
      <SectionContainer>
        <SectionContentList>
          <SectionListItemButton>Item 1</SectionListItemButton>
          <SectionListItemButton actionsArea={
              <ButtonIcon
                icon={IconEditOutlinedFilled}
                variant={ButtonVariant.Transparent}
                tone={Tone.Neutral}
                size={Size.Sm}
              />
            }>
              Item 2
            </SectionListItemButton>
          <SectionListItemButton>Item 3</SectionListItemButton>
        </SectionContentList>
      </SectionContainer>
    </Section>
  </Story>
</Canvas>
