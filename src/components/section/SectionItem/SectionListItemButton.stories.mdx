import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs'
import {
  Section,
  SectionContainer,
  SectionContentList,
  SectionListItemButton,
} from '../'

<Meta
  title="Components/Section/SectionListItemButton"
  argTypes={{ onClick: { action: 'onClick' } }}
  component={SectionListItemButton}
/>

# SectionListItemButton

This component adds an HTML-button element and applies basic styling.

export const Template = ({ children, ...args }) => {
  return <SectionListItemButton {...args}>{children}</SectionListItemButton>
}

<Canvas>
  <Story name="Default" args={{ children: 'List item button' }}>
    {Template.bind({})}
  </Story>
</Canvas>

### Props

<ArgsTable story="Default" />

### Theme

- `section.listItemButton.base`: Define the base style of the component.
- `section.listItemButton.action`: Define the base style of the action.

## Example: Usage within Section

<Canvas>
  <Story name="Usage within Section">
    <Section>
      <SectionContainer>
        <SectionContentList>
          <SectionListItemButton>Item 1</SectionListItemButton>
          <SectionListItemButton>Item 2</SectionListItemButton>
          <SectionListItemButton>Item 3</SectionListItemButton>
        </SectionContentList>
      </SectionContainer>
    </Section>
  </Story>
</Canvas>
