import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs'
import { SectionContent } from '../Section/SectionContent'
import { SectionDescriptionItem } from './SectionItem'

<Meta
  title="Components/Section/SectionDescriptionItem"
  component={SectionDescriptionItem}
/>

# SectionDescriptionItem

The SectionDescriptionItem displays read only information with HTML description list element (`dl`) tag.

export const Template = ({ title, content }) => (
  <div className="bg-gray-700">
    <SectionDescriptionItem title={title} content={content} />
  </div>
)

<Canvas>
  <Story name="Default" args={{ title: 'Name', content: 'John Doe' }}>
    {Template.bind({})}
  </Story>
</Canvas>

### Props

<ArgsTable story="Default" />

### Theme

- `section.sectionDescriptionItem.base`: Define the base style of the component, e.g., text color or background color.

## Example: Usage within Section

<Canvas>
  <Story name="Usage within Section">
    <SectionContent>
      <SectionDescriptionItem content="Name" title="John Doe" />
    </SectionContent>
  </Story>
</Canvas>
