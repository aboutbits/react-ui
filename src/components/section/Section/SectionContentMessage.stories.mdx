import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs'
import IconWarning from '@aboutbits/react-material-icons/dist/IconWarning'
import { Tone } from '../../types'
import { SectionContentMessage } from './SectionContentMessage'
import {
  SectionContentEmpty,
  SectionContentError,
} from './ConvenientSectionContentMessage'

<Meta
  title="Components/Section/SectionContentMessage"
  component={SectionContentMessage}
/>

# SectionContentMessage

This component can be used to show a message to a user in a [SectionContent](/docs/components-section-sectioncontent--default-story).
The message will be centered in the default theme, it is conducted of a title, text, and icon all of which are optional to be combined flexibly. Furthermore, it is possible to set a tone for all the different type of messages (success, critical, etc.)

export const Template = ({ ...args }) => <SectionContentMessage {...args} />

<Canvas>
  <Story
    name="Default"
    args={{
      text: 'No users found in database',
      tone: Tone.primary,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

### Props

<ArgsTable story="Default" />

### Theme

- `section.contentMessage.container.base`: Defines base styles of the container
- `section.contentMessage.container.wrapper`: Defines base styles of the wrapper of the container
- `section.contentMessage.icon.base`: Defines base styles of the icon
- `section.contentMessage.icon.tone[$tone]`: Defines custom styles per tone of the icon
- `section.contentMessage.iconContainer.base`: Defines base styles of the icon background
- `section.contentMessage.iconContainer.tone[$tone]`: Defines custom styles per tone of the icon background
- `section.contentMessage.title.base`: Defines base styles of the title
- `section.contentMessage.title.tone[$tone]`: Defines custom styles per tone of the title
- `section.contentMessage.text.base`: Defines base styles of the test
- `section.contentMessage.text.tone[$tone]`: Defines custom styles per tone of the text

## Title

{' '}

<Canvas>
  <Story
    name="Default"
    args={{
      title: 'Server Error',
      tone: Tone.critical,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Text

<Canvas>
  <Story
    name="Default"
    args={{
      text: 'The server could not handle the request! Try again',
      tone: Tone.critical,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Icon

<Canvas>
  <Story
    name="Default"
    args={{
      icon: IconWarning,
      tone: Tone.critical,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Convenient alert components

<Canvas>
  <Story name="SectionContentError">
    <SectionContentError
      title="Server Error"
      text="The server could not handle the request! Try again"
    />
  </Story>
  <Story name="SectionContentEmpty">
    <SectionContentEmpty
      title="No user found"
      text="There are no items in this list at the moment."
    />
  </Story>
</Canvas>
