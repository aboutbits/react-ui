import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs'
import { SectionContentMessage } from './SectionContentMessage'

<Meta
  title="Components/Section/SectionContentMessage"
  component={SectionContentMessage}
/>

# SectionContentMessage

This component can be used to show a message to a user in a [SectionContent](/docs/components-section-sectioncontent--default-story).
The message will be centered in the default theme.

export const Template = ({ children }) => (
  <SectionContentMessage>{children}</SectionContentMessage>
)

<Canvas>
  <Story name="Default" args={{ children: 'No users found in database.' }}>
    {Template.bind({})}
  </Story>
</Canvas>

### Props

<ArgsTable story="Default" />

### Theme

- `section.contentMessage.container.base`: Define the styles of the section content container.
