import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs'
import IconList from '@aboutbits/react-material-icons/dist/IconList'
import { Tone } from '../../types'
import { SectionContentNotification } from './SectionContentNotification'
import {
  SectionContentEmpty,
  SectionContentError,
  SectionContentMessage,
} from './ConvenientSectionContentNotification'

<Meta
  title="Components/Section/SectionContentNotification"
  component={SectionContentNotification}
/>

# SectionContentNotification

export const Template = ({ ...args }) => (
  <SectionContentNotification {...args} />
)

<Canvas>
  <Story
    name="Default"
    args={{
      icon: IconList,
      title: 'No users found',
      text: 'There are no items in this list at the moment.',
      tone: Tone.success,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

### Props

<ArgsTable story="Default" />

### Theme

## Convenient alert components

<Canvas>
  <Story name="SectionContentError">
    <SectionContentError
      title="Server Error"
      text="The server could not handle the request! Try again"
    />
  </Story>
</Canvas>

## Convenient alert components

<Canvas>
  <Story name="SectionContentEmpty">
    <SectionContentEmpty
      title="No user found"
      text="There are no items in this list at the moment."
    />
  </Story>
</Canvas>

## Convenient alert components

<Canvas>
  <Story name="SectionContentMessage">
    <SectionContentMessage text="No users found in database." />
  </Story>
</Canvas>
