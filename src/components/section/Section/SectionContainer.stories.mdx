import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs'
import { SectionContainer, SectionContent } from '../'

<Meta
  title="Components/Section/SectionContainer"
  component={SectionContainer}
/>

# SectionContainer

In the default theme, this component is used to wrap the `SectionContent` and the `SectionFooter` with a border and rounded corners.

export const Template = ({ children, ...args }) => (
  <SectionContainer {...args}>{children}</SectionContainer>
)

<Canvas>
  <Story
    name="Default"
    args={{
      children: <SectionContent>Default Section Container</SectionContent>,
    }}
    argTypes={{
      children: { control: false },
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

### Props

<ArgsTable story="Default" />

### Theme

- `section.container.base`: Define the styles of the section container.
