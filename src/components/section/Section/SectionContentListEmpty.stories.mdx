import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs'
import { SectionContentListEmpty } from './SectionContentList'

<Meta
  title="Components/Section/SectionContentListEmpty"
  component={SectionContentListEmpty}
/>

# SectionListItem

This component can be used to show the user that there is no data available in this list.
It is a utility component based on [SectionListItem](/docs/components-section-sectionlistitem--default-story).

export const Template = ({ children }) => (
  <SectionContentListEmpty>{children}</SectionContentListEmpty>
)

<Canvas>
  <Story name="Default" args={{ children: 'No users found in database.' }}>
    {Template.bind({})}
  </Story>
</Canvas>

### Props

<ArgsTable story="Default" />

### Theme

- `section.contentList.empty`: Define the base style of them empty section content list.
