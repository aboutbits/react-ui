import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs'
import { SectionPaginationContainer } from './SectionPagination'

<Meta
  title="Components/Section/SectionPaginationContainer"
  component={SectionPaginationContainer}
/>

# SectionPaginationContainer

The SectionPaginationContainer-component is used as a root-component for the pagination e.g., for a search.

export const Template = ({ children, ...args }) => (
  <SectionPaginationContainer {...args}>{children}</SectionPaginationContainer>
)

<Canvas>
  <Story name="Default" args={{ className: 'bg-primary' }}>
    {Template.bind({})}
  </Story>
</Canvas>

### Props

<ArgsTable story="Default" />

### Theme

- `section.pagination.container.base`: Define the flexbox and the padding of component.
