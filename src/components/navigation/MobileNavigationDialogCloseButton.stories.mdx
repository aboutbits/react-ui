import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs'
import { NavigationProvider } from './NavigationContext'
import { MobileNavigationDialogCloseButton } from './MobileNavigationDialogCloseButton'

<Meta
  title="Components/Navigation/MobileNavigationDialogCloseButton"
  component={MobileNavigationDialogCloseButton}
  decorators={[
    (Story) => (
      <div className="relative h-20 w-full bg-neutral-100 text-black">
        <Story />
      </div>
    ),
  ]}
/>

# MobileNavigationDialogCloseButton

This component is used to provide a close button for the mobile menu dialog window.

export const Template = ({ ...args }) => (
  <NavigationProvider>
    <MobileNavigationDialogCloseButton {...args} />
  </NavigationProvider>
)

<Canvas>
  <Story name="Default">{Template.bind({})}</Story>
</Canvas>

### Props

<ArgsTable story="Default" />

### Theme

- `navigation.mobileDialogCloseButton.base`
- `navigation.mobileDialogCloseButton.icon.base`
