import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs'
import IconLogout from '@aboutbits/react-material-icons/dist/IconLogout'
import { NavigationItemButton } from './NavigationItemButton'

<Meta
  title="Components/Menu/NavigationItemButton"
  component={NavigationItemButton}
/>

# NavigationItemButton

This button can be placed inside the navigation. E.g. Logout

export const Template = ({ ...args }) => (
  <NavigationItemButton {...args} content="Log out" icon={IconLogout} />
)

<Canvas>
  <Story name="Default">{Template.bind({})}</Story>
</Canvas>

### Props

<ArgsTable story="Default" />

### Theme

- `navigation.item.base`: Define the padding for the button.
- `navigation.item.normal`: Define the background color and interaction for hover and focus
- `navigation.item.icon.base`: Define the size of the icon.
- `navigation.item.content.base`: Define the floating direction of the content.

## Examples

<Canvas>
  <Story name="Very long text">
    <NavigationItemButton
      content={'Click here to log out from your account.'}
      icon={IconLogout}
    />
  </Story>
  <Story name="Disabled">
    <NavigationItemButton
      disabled={true}
      content={'Disabled'}
      icon={IconLogout}
    />
  </Story>
</Canvas>
