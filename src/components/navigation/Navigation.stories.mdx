import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs'
import IconArrowDropDownCircle from '@aboutbits/react-material-icons/dist/IconArrowDropDownCircle'
import { Navigation } from './Navigation'
import { NavigationItemButton } from './NavigationItemButton'

<Meta title="Components/Menu/Navigation" component={Navigation} />

# Navigation

This component is used to provide an HTML-`nav` element for a menu.

export const Template = ({ ...args }) => (
  <Navigation navLabel="Navigation" {...args}>
    <NavigationItemButton text="Button 1" icon={IconArrowDropDownCircle} />
    <NavigationItemButton text="Button 2" icon={IconArrowDropDownCircle} />
    <NavigationItemButton text="Button 3" icon={IconArrowDropDownCircle} />
  </Navigation>
)

<Canvas>
  <Story name="Default">{Template.bind({})}</Story>
</Canvas>

### Props

<ArgsTable story="Default" />

### Theme

- `menu.container.base`: Define the flexbox and padding between each child element.
