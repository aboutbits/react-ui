import { yupResolver } from '@hookform/resolvers/yup'
import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs'
import { action } from '@storybook/addon-actions'
import { useForm } from 'react-hook-form'
import * as Yup from 'yup'
import { Form } from './Form'
import { SelectYearFormField } from './SelectYearFormField'

<Meta
  title="Components/React Hook Form/SelectYearFormField"
  component={SelectYearFormField}
  decorators={[
    (Story) => {
      const resolver = yupResolver(
        Yup.object({
          year: Yup.string().required('Required'),
        })
      )
      const form = useForm({
        resolver,
        defaultValues: { year: '' },
        mode: 'onTouched',
      })
      return (
        <Form form={form} onSubmit={action('onSubmit')}>
          <Story />
        </Form>
      )
    },
  ]}
/>

# SelectYearFormField

A [SelectFormField](../?path=/docs/components-reacthookform-selectformfield--default-story) for years.

export const Template = ({ ...args }) => <SelectYearFormField {...args} />

<Canvas>
  <Story
    name="Default"
    args={{
      name: 'year',
      label: 'Year of birth',
      from: 2018,
      to: new Date().getFullYear(),
    }}
    argTypes={{
      disabled: { type: 'boolean' },
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

### Props

<ArgsTable story="Default" />
