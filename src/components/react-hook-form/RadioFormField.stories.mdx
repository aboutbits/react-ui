import { yupResolver } from '@hookform/resolvers/yup'
import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs'
import { action } from '@storybook/addon-actions'
import { useForm } from 'react-hook-form'
import * as Yup from 'yup'
import { Form } from './Form'
import { RadioFormField } from './RadioFormField'

<Meta
  title="Components/React Hook Form/RadioFormField"
  component={RadioFormField}
  decorators={[
    (Story) => {
      const formSchema = Yup.object({
        acceptPrivacy: Yup.boolean(),
      })
      const defaultValues = {
        acceptPrivacy: false,
      }
      const form = useForm({
        resolver: yupResolver(formSchema),
        defaultValues,
      })
      return (
        <Form form={form} onSubmit={action('onSubmit')}>
          <Story />
        </Form>
      )
    },
  ]}
/>

# RadioFormField

A [Radio](../?path=/docs/components-form-primitive-radio--default-story) within the context of a `react-hook-form` form.

export const Template = ({ ...args }) => <RadioFormField {...args} />

<Canvas>
  <Story
    name="Default"
    args={{
      name: 'privacy',
      label: 'Accept privacy policy',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

### Props

<ArgsTable story="Default" />
